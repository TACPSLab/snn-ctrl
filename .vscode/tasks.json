{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build workspace",
            "type": "shell",
            "command": "colcon build --symlink-install",
            "problemMatcher": [],
            "options": {"cwd": "${workspaceFolder}/../"},
        }
    ]
}